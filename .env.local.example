# API base URL for the ISBAT ERP backend.
# Leave empty for same-origin requests (when Next.js is behind the same host as the API).
# Set to the full URL (with no trailing slash) when running against a separate backend.
NEXT_PUBLIC_API_BASE_URL=https://erp.isbatuniversity.ac.ug
