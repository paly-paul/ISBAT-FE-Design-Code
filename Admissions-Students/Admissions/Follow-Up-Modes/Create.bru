meta {
  name: Create Follow-Up Mode
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/admissions/followup-modes
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "followUpModeName": "Phone Call"
  }
}

docs {
  Creates a new follow-up mode.

  Success (201): data = created FollowUpModeDto.
  Failure: 400 `validation_error`.
}
